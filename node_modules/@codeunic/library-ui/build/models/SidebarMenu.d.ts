export declare class SidebarMenuItemHeader implements ISidebarMenu {
    name: string;
    icon: string;
    constructor(name: string, icon: string);
}
export declare class SidebarMenuItemTitle implements ISidebarMenu {
    name: string;
    onClick: () => any;
    icon: string;
    constructor(name: string, onClick: any);
}
export interface ISidebarMenu {
    name: string;
    icon: string;
}
export declare class SidebarMenuItemGroup implements ISidebarMenu {
    icon: string;
    name: string;
    constructor(name: string, icon: string);
}
export declare type TOnline = "online" | "away" | "offline";
export declare class SidebarMenuItemChat implements ISidebarMenu {
    icon: string;
    name: string;
    online: TOnline;
    number: number;
    active: boolean;
    constructor(name: string, icon: string, online: TOnline, number: number, active?: boolean);
}
