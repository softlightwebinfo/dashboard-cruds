/// <reference types="react" />
import { FormLabelInterface } from "./FormLabelInterface";
import { FormComponentModel } from "./FormComponentModel";
import { IFormComponent } from "./IFormComponent";
import { IChildren } from "../../Interfaces";
export declare class FormLabel extends FormComponentModel implements FormLabelInterface, IFormComponent {
    children: IChildren;
    className: string;
    htmlFor: string;
    isUp: boolean;
    constructor(props?: FormLabelInterface);
    render(index?: string): JSX.Element;
}
