import { IFormComponent } from "./IFormComponent";
import { IChildren, IStyle } from "../..";
import React from "react";
import { IFormGroupComponentProps } from "../../components/FormGroupComponent/FormGroupComponent.types";
import { FormComponentModel } from "./FormComponentModel";
export interface FormGroupInterface extends IFormGroupComponentProps {
}
export declare class FormGroup extends FormComponentModel implements IFormComponent, FormGroupInterface {
    private components;
    children: IChildren | React.ReactElement[];
    className: string;
    form: boolean;
    row: boolean;
    style: IStyle;
    constructor(props?: IFormGroupComponentProps);
    Add(component: IFormComponent): this;
    render(index?: string): JSX.Element;
}
