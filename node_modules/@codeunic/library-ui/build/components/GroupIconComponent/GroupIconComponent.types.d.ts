import { IProp } from "../../Interfaces/IProp";
import { ReactElement } from "react";
export interface IGroupIconComponentProps extends IProp {
    icon: ReactElement;
    content: ReactElement;
}
